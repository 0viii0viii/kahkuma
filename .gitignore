node_modules
dist
dist-ssr
*.local
.vercel
.DS_Store

# Raw high-res source sculpts (74MB) — kept local only; keep your own backup.
# Optimized versions in public/models/ are committed and used for the build.
raw-models
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# JetBrains IDE
.idea
